pipeline {

  stages { 
    stage('Testing') {
      steps{ 
        echo 'Start running Automated tests.'
      } 
    } 
   stage('Build the image') {
     steps {
        echo 'Start cloning the reposity'
      }
   }

  } 
}
