spring:
  sql:
    init:
      mode: always
  datasource:
    username: dev
    password: password
    url: jdbc:mysql://localhost:3306/ums-db
  jpa:
    hibernate:
      ddl-auto: validate
    database: mysql
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect