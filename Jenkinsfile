pipeline {
    agent {
    	label 'ums-builder'
    }

    stages {
        stage('Clone repository') {
            steps {
                echo 'Start cloning the reposity'
            }
        }
    }
}
